
type Query {
    bookById(id: ID): Book
    officesById(id: ID): Offices
}

type Offices {
     officeCode: String!
     city: String!

}

type Book {
    id: ID
    name: String
    pageCount: Int
    author: Author
}

type Author {
    id: ID
    firstName: String
    lastName: String
}